std::cout << "The sum of " << v1;
          << " and " << v2;
          << " is " << v1 + v2 << std::endl;
       
//Illegal, the ; at the end of line one ends the statement, making line two a completely standalone statement.
//getting rid of the ; on lines 1 and 2 would fix it.
