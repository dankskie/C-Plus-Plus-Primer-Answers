//It counts how many times the integer occurs, tries to read the next cin but since there is none, it stops and returns 0.
//It initializes cnt, skips the while, then prints line 18/19 and returns 0.
