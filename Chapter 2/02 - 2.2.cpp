//I don't even know what a mortgage is.
//I would assume defaulting to unsigned if the values will never go negative, using double for precision when applicable,
//and matching types to fit around the expected max and min of the 3 values.
